class Solution {
public:
    bool uniformArray(vector<int>& nums1) {
        return true;
        // int cnt_o =0;
        // int cnt_e =0;
        // for(int i=0;i<nums1.size();i++)
        // {
        //    if(nums1[i]%2==0) cnt_e++;
        //    else cnt_o++;
        // }
        // if(cnt_o==0 || cnt_e==0) return true;

        // if(cnt_o == cnt_e || cnt_o==2*cnt_e || cnt_e==2*cnt_o) return true;
        // else return false;
    }
};